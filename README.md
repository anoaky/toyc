# TCC (ToyC Compiler)

This project implements a compiler for a toy language, a subset of C. Translation from Java of a project completed in uni.

# Running Tests

Run `./run_tests.sh -h` for more information on running the test suite.